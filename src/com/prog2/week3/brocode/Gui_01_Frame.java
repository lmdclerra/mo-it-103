package com.prog2.week3.brocode;

public class Gui_01_Frame {

    public static void main(String[] args) {
        /*
        JFrame frame = new JFrame("JFrame title goes here");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setResizable(false); // Prevents resizing of the window
        frame.setSize(420, 420); // Sets the width and height of the window
        frame.setVisible(true); // Makes the window visible
        frame.setLocationRelativeTo(null); // Centers the window on the screen

        ImageIcon icon = new ImageIcon("src/com/prog2/week3/brocode/myIcon.png"); // Path to the image file
        frame.setIconImage(icon.getImage()); // Sets the icon of the window
        frame.getContentPane().setBackground(new java.awt.Color(255, 255, 255)); // Sets the background color of the window
        */
        
        new MyTextFieldFrame(); // Create an instance of MyFrame to display the GUI
    }
}
